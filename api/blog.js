const createPost = (someText) => {
    var myObject = {
        ID: 474,
        BlogText: "Hello my name is Mike!"
    };

    console.log("ready to send to dynamodb!");
}

module.exports = {
    createPost
}